{
  "identifier": "default",
  "description": "Core capability for the main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "shell:allow-spawn",
    "shell:allow-execute"
  ]
}
