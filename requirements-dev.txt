# Development install shim.
#
# pyproject.toml is the single source of truth for development dependencies.
# This file exists only for tools and docs that still call:
#   pip install -r requirements-dev.txt
-e .[dev,docs]
