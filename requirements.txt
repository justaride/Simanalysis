# Runtime install shim.
#
# pyproject.toml is the single source of truth for package dependencies.
# This file exists only for tools and docs that still call:
#   pip install -r requirements.txt
-e .
